{
  "name": "actdata-plugins",
  "private": true,
  "type": "module",
  "description": "ACT Data's plugin marketplace for Claude Code, ChatGPT, Codex, and GitHub Copilot.",
  "scripts": {
    "verify": "sh scripts/verify-all.sh",
    "test": "sh scripts/tests/run-tests.sh",
    "check:size": "bun scripts/check-size.ts .",
    "check:binaries": "bun scripts/check-no-binaries.ts ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
